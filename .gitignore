# Dependencies
node_modules/
client/node_modules/

# Local environment variables (Do NOT commit keys to Git!)
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
lerna-debug.log*

# OS-generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Temporary / Backups
client_backup/

# Runtime data files with sensitive content (passwords, sessions, API keys)
# These are generated at runtime on the server — never commit them
tenants.json
raffle.json

# User Uploaded Assets
uploads/

# IDE / Editors
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
